# Streamlit Cloud secrets — paste into app Settings → Secrets
# Direct mode (recommended): no separate FastAPI server needed

USE_API = false

LLM_PROVIDER = "groq"
GROQ_API_KEY = ""
OPENAI_API_KEY = ""
GEMINI_API_KEY = ""
HF_TOKEN = ""

PROTOCOL_BUFFERS_PYTHON_IMPLEMENTATION = "python"

# Optional — remote FastAPI on Render / Railway / Fly.io / etc.
# USE_API = true
# API_URL = "https://your-fastapi-backend.onrender.com"
